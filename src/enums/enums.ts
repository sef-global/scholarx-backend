export enum ProfileTypes {
    DEFAULT = "default",
    ADMIN = "admin"
}

export enum EmailStatusTypes {
    SENT = "sent",
    DELIVERED = "delivered",
    FAILED = "failed"
}

export enum MenteeStateTypes {
    PENDING = "pending",
    REJECTED = "rejected",
    ACCEPTED = "accepted"
}
